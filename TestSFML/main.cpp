#include <SFML/Graphics.hpp>

int main()
{
    App* app = new App();

    while(app->run()){};

    app->stop();
            if (event.type == sf::Event::Closed)
                window.close();
        }
            if (event.type == sf::Event::Closed)
                window.close();
        }

    delete(app);

    return 0;
}